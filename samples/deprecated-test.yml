openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths:
  /test:
    get:
      deprecated: true
      responses:
        '200':
          description: Success
    post:
      responses:
        '200':
          description: Success
components:
  schemas:
    TestSchema:
      type: object
      properties:
        active_field:
          type: string
        deprecated_field:
          type: string
          deprecated: true
