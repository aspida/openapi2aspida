/* eslint-disable */
export type Methods = {
  /** Check if the first admin user has already been registered */
  get: {
    status: 200

    /** response */
    resBody: {
      foo: string
    }
  }
}
